{
  "name": "kasen",
  "description": "Underscore-like functional utilities based on ES2015 Generator",
  "version": "0.0.1",
  "author": "Yuya Takeyama <sign.of.the.wolf.pentagram@gmail.com>",
  "devDependencies": {
    "babel-polyfill": "^6.16.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-latest": "^6.16.0",
    "babel-preset-power-assert": "^1.0.0",
    "chokidar": "^1.6.1",
    "mocha": "^3.1.2",
    "power-assert": "^1.4.1"
  },
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "NODE_ENV=production babel src --out-dir lib --source-maps inline",
    "prepublish": "npm run build",
    "test": "mocha test/*.js",
    "watch": "babel src --out-dir lib --watch --source-maps inline"
  },
  "dependencies": {
    "babel-cli": "^6.18.0"
  }
}
